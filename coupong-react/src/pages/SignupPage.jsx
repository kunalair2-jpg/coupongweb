// Replaced by Clerk's built-in /sign-up flow — this page redirects there.
import { Navigate } from 'react-router-dom';
export default function SignupPage() {
    return <Navigate to="/sign-up" replace />;
}
